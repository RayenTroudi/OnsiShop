import CartPage from '@/components/cart/CartPage';

export const metadata = {
  title: 'Shopping Cart - ONSI',
  description: 'Review your cart items and proceed to checkout'
};

export default function Page() {
  return <CartPage />;
}
